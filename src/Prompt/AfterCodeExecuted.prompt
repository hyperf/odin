User Requirement:
```
{{userRequirement}}
```
Code:
```
{{code}}
```
The code executed result:
```
{{codeExecutedResult}}
```
Requirements:
1. If the code executed result is successful, then response the user with the code executed result, with `Final Result: ` format.
2. If the code executed result is failed, then use `run_code` function call and try to fix the error.